This empty file tells GitHub Pages to serve these files as plain static assets
instead of passing them through Jekyll. Jekyll skips files and folders whose
names start with "_" or ".", which would silently 404 on the live site.
